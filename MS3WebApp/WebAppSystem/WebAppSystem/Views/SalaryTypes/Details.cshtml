@model WebAppSystem.Models.SalaryType

@{
    ViewData["Title"] = "Salary Type Details";
}

<div class="row justify-content-center mt-4">
    <div class="col-md-10">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h2 class="mb-0"><i class="bi bi-currency-dollar"></i> Salary Type Details</h2>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">Salary Type ID</dt>
                    <dd class="col-sm-9">@Model.SalaryTypeId</dd>
                    
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Type)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Type)</dd>
                    
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.PaymentFrequency)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.PaymentFrequency)</dd>
                    
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Currency)</dt>
                    <dd class="col-sm-9">
                        @if (!string.IsNullOrEmpty(Model.Currency))
                        {
                            <span class="badge bg-primary">@Model.Currency</span>
                        }
                        else
                        {
                            <span class="text-muted">N/A</span>
                        }
                    </dd>
                </dl>
                
                <hr class="my-4" />
                
                <h5 class="mb-3"><i class="bi bi-people"></i> Employees with this Salary Type</h5>
                @if (ViewBag.AssignedEmployees != null && ViewBag.AssignedEmployees.Count > 0)
                {
                    <div class="table-responsive">
                        <table class="table table-sm table-dark">
                            <thead>
                                <tr>
                                    <th>Employee ID</th>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var employee in ViewBag.AssignedEmployees)
                                {
                                    <tr>
                                        <td>@employee.EmployeeId</td>
                                        <td>@employee.FullName</td>
                                        <td>@employee.Email</td>
                                        <td>
                                            <a asp-controller="Employees" asp-action="Details" asp-route-id="@employee.EmployeeId" 
                                               class="btn btn-sm btn-outline-info" title="View Employee">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <p class="text-muted"><small>Total: @ViewBag.AssignedEmployees.Count employee(s)</small></p>
                }
                else
                {
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle"></i> No employees currently assigned to this salary type.
                    </div>
                }
                
                <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                    @if (Context.Session.GetString("UserRoles")?.Contains("System Administrator") == true)
                    {
                        <a asp-action="Edit" asp-route-id="@Model.SalaryTypeId" class="btn btn-warning">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                        <a asp-action="Delete" asp-route-id="@Model.SalaryTypeId" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Delete
                        </a>
                    }
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Back to List
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
